function FindBalPoint(arr){
  var sum = 0;
  for(var i=0; i < arr.length; i++){
    sum +=arr[i];
  }
  var mid = sum/2;
  for var( k=0; k<arr.length; k++){
    var sum2 += arr[i];
    if(sum2 == half){
      return true;
    }
  }
  return false;
}
